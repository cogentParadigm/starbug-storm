{% set attributes = attributes|default({}) %}
{% set attributes = attributes|merge({"class":((attributes.class is empty) ? "" : attributes.class ~ " ") ~ "btn btn-brand-blue"}) %}
{{ include("button.html.twig") }}